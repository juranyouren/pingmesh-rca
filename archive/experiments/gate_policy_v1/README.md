# Historical Gate-Policy Wrappers

These wrappers evaluated the fixed `baseline`, `strict_combined`, and
`conservative` policies. They were superseded by `configurable_gate_v1` and the
offline `gate_auto` search. They remain here only for traceability.
