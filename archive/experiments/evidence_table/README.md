# Historical Evidence-Table Experiments

This directory preserves the former M1/M2/M3 precomputed-evidence workflow.
It is retained for research traceability and is not an active runtime path.

The files preserve their historical imports. Restore them to `scripts/` from
Git history if an exact reproduction of that workflow is required. Current RCA
experiments use `scripts/run_rca_experiments.sh`.
