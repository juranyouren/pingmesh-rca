# tmp

The active thesis pipeline should not depend on files in this directory.

One-off maintenance and diagnostic scripts were moved to `archive/tmp_tools/`.
Keep new thesis experiments under `scripts/run_paper_*.sh`, reusable code under
`Sys/`, and written thesis context under `docs/`.
