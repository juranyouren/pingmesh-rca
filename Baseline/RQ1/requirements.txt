# Install in a dedicated server environment. Keep an existing Ascend torch build.
# These compatibility ranges are not a tested lockfile; save pip freeze per run.
numpy>=1.23,<2
scipy>=1.10,<1.14
pandas>=1.5,<2.2
# NetworkX 3.5+ requires Python >=3.11. Keep the RQ1 Python 3.10 target.
networkx==3.4.2
tigramite>=5.2,<6
gcastle==1.0.4
pytest>=7,<9
# NEC additionally requires torch; use the server's approved CPU/NPU build.
