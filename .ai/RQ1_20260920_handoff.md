# 2026-09-20 RQ1 脚本交付

用户授权：实现 RQ1 脚本，服务器数据由用户自行运行；THP 需参考论文实现。
本次不恢复旧研究任务，不覆盖已有 CURRENT_TASK/HANDOFF 等未提交修改。

新增 `Baseline/RQ1/`（方法适配、图投影、统一评价、CLI、测试和说明）、
`configs/baselines/rq1.json`、`scripts/run_rq1.sh`。
默认五方法 TimeOrder / NEC mechanism reproduction / PCMCI / THP-TTPM / current P0。
Shared 与 Oracle 分离；rooted/topology 双视图；Adj/AH 独立掩码、完整 SHD、事故宏平均和失败记账。
THP 调用 gCastle TTPM，保存类型矩阵，显式映射设备边，排除上游固定对角项。
推断与训练只有公共白名单观测；Oracle 包装层仅注入确认根；不调用外部 LLM。

论文和上游接口已定点阅读；详见 `Baseline/RQ1/README.md` 的来源与适配限制。
曾尝试现有 SSH 别名，连接超时；用户随后明确服务器自行运行，因此不继续连接或部署。
**7 个 Python 文件与配置 JSON 静态语法检查通过；测试与真实实验尚未执行，服务器结果 UNKNOWN**。
提交状态：按用户要求整理为独立本地提交；未推送。
仓库原有大量未提交修改／删除未恢复、未纳入本次变更。

服务器验证命令：

```bash
python -m pytest Baseline/RQ1/tests -q
RQ1_NUMERICAL_SMOKE=1 python -m pytest Baseline/RQ1/tests -q
```

正式运行参数、输入及 canonical labels / reviewed groups / OOF roots 合同见 README。
当前 Ours 是 common-input 固定根 P0，不能冒称历史端到端完整系统结果。

## 2026-09-21 零参数服务器入口

按用户补充修改：`bash scripts/run_rq1.sh` source common.sh，默认读取
PINGMESH_DATA 和 PINGMESH_PROPAGATION_LABELS_ROOT，结果自动保存到 PINGMESH_RESULTS。
默认明确为 Oracle；GT 直接支持 `<case_id>/propagation_label.json`。
仅 definite 为正、explicit_no_direct 为负、possible 未判定；不推断完整标注。
自动分组标记为未核实，禁用 CI 和 NEC 训练；提供核实分组/manifest 后启用 NEC。
新增 `--check-inputs` 不需要 GT，不运行模型、不写实验结果。

验证：8 项 dependency-free unittest 通过（含合成 GT 目录端到端及重评分）；
本地两个真实示例只读加载成功：22/11 个设备、16/13 条物理边、2/12 条窗口内事件。
本地无传播 GT，未运行真实数据推断、评分、训练；完整 pytest/数值后端仍待服务器。

### 重复告警 ID 修复

用户报告服务器 `Conflicting records share event_id alarm:...`。
原生成规则仅使用 source/device/alarm_id，复用 alarm_id 的不同观测触发冲突。
改为加上白名单观测 SHA-256；重复观测去重，不同时间/内容保留；显式 event_id 冲突仍拒绝。
增加 reused_alarm_id_groups 诊断。生成 ID 与原始输入指纹改变，旧冻结 manifest 需要重建。
9 项身份回归测试与 8 项准备/端到端测试全部通过（共 17 项），未运行服务器真实实验。

### Python 3.10 依赖入口纠正

用户提供的安装错误指定 networkx==3.6.1，定位到历史 `Baseline/requirements-repro.txt`
（Windows/Python 3.12 快照），而不是 RQ1 requirements。官方 PyPI 要求 3.6.1 使用 Python>=3.11。
保留历史快照版本，只添加适用范围说明；RQ1 固定 networkx==3.4.2，双 README 明确正确安装入口。
RQ1 requirements 的 pip dry-run（--ignore-installed --only-binary=:all: --python-version 3.10）
对 manylinux2014_x86_64 和 manylinux2014_aarch64 均退出 0；只验证候选包/依赖解析，
未实际安装目标 Python 环境，未验证 PyTorch/NPU 或模型运行。报告保存在忽略的 tmp/ 下。

### PCMCI collection_coverage_unknown

保留默认严格覆盖策略，新增显式 `--pcmci-coverage record-count` 与 common.sh 中的
PINGMESH_RQ1_PCMCI_COVERAGE 开关。无覆盖元数据时可分析导出事件计数，0 不表示设备健康
或完整采集；不修改输入覆盖字段，已知采集缺口仍掩码。配置、方法名和结果表明确标注策略。
失败预测也保留配置，便于解释不适用原因；错误信息提示可用开关。
6 项新覆盖策略测试 + 17 项既有 unittest 全通过（23 项）。未运行真实数据实验或数值后端。
