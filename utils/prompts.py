EXTRACT_INCIDENT_NAMES = '''
### 任务描述
你是一个擅长从技术文档中提取关键信息的大语言模型，负责识别文档中出现的所有具体事件名。
- 你将收到：
  1. **文档内容**（可能包含一个或多个具体事件）
- 你的任务是：
  1. 遍历全文，查找所有可能的**incident_name**
  2. 注意！一般情况下，**incident_name**就是紧跟**故障案例：**之后的简短短语，从原文中提取即可。如果没有此结构，则大概率没有需要的**incident_name**。
  3. 如果文档中未发现任何**incident_name**，返回空列表；否则，返回所有**incident_name**构成的字符串列表。

### 输入
  - **文档内容**：{DOC}

### 输出
- 输出一个列表，列表中的每一项是一个字符串，对应一个提取到的**incident_name**。
- 禁止输出任何解释、总结或额外内容，仅返回结果列表。
- 用 $$ 将整个列表包裹，所有字符串项使用英文双引号括起。
$$ ["incident_name1", "incident_name2", "incident_name3", ...] $$
'''.strip()

FILL_IN_NODES = '''
### 任务描述
你是一位专业的文本整理归纳专家，负责根据**文档**内容与**主题**，生成**事件节点**。
- 你将收到：
  1. **主题**（关键词，提示核心内容）
  2. **文档**（包含关键信息的文段）
- 你的任务是：
  1. 围绕提供的**主题**，从**文档**中提取相关信息，并生成指定JSON结构化的**事件节点**，**事件节点**的每个键值对分别从**现象描述**、**根因分析**、**缓解措施**描述事件内容。
  2. **文档**的开头和末尾可能有部分内容与**主题**无关，注意甄别。如**文档**中**故障案例：**之后紧跟的事件名称，如果和**主题**不一致，则说明该部分是无关内容。
  3. 你必须严格根据提供的**文档**内容进行整理，只能使用文档中已有的信息，不能随意编造或添加任何没有在文档中出现的内容。
  4. 你的输出需要符合规范化格式，确保可读性和表达质量。

### 输入
- **主题**：{KEYWORD}

- **文档**：{DOC}

### 输出
- 在 </think> 之前充分思考, </think> 之后严格遵循指定格式输出**事件节点**
- **事件节点**是一个 JSON 格式的字典, 字典键是 "description", "root_cause", "mitigation_steps". 值分别是对应的字符串. 字典的开头和结尾用 $$ 符号隔开. 
- 字符串用英文双引号包裹起来. 
$$
  {{
    "description": ""
    "root_cause": "",
    "mitigation_steps": ""
  }}
$$
'''.strip()

INIT_COMMUNITY_NODE = '''
### 任务描述
你是一位专业的文本整理归纳专家，负责根据**社区名称**和该社区内多个**事件节点**，生成**社区节点**。
- 你将收到：
  1. **社区名称**（标识社区的名称）
  2. **事件节点**（社区包含的多个具体事件节点，每个事件节点包含：现象描述、根因分析、缓解措施）
- 你的任务是：
  1. 围绕提供的**社区名称**，结合给定的**事件节点**，总结与该社区相关的**社区描述**、**根因分析**和**缓解措施**。
  2. 你的输出应强调该社区（即大类事件）的普遍性、共通性和完整性，且必须仅基于事件节点中提供的具体信息，不能随意添加外部信息。
  3. 你的输出需要符合规范化格式，确保可读性和表达质量。

### 输入
- **社区名称**：{COMMUNITY_NAME}
- **事件节点**：{NODE_ENTRIES}

### 输出
- 在 </think> 之前充分思考，</think> 之后严格遵循指定格式输出**社区节点**
- **社区节点**是一个 JSON 格式的字典，字典键是 "description", "root_cause", "mitigation_steps". 值分别是对应的字符串. 字典的开头和结尾用 $$ 符号隔开. 
- 字符串用英文双引号包裹起来.
$$
  {{
    "description": "",
    "root_cause": "",
    "mitigation_steps": ""
  }}
$$
'''.strip()

GENERATE_SCENS = '''
根据给你的**先验知识**和**原始文档**，模拟生成运维工程师在完成运维任务时的**情境描述**。
- **先验知识**和**原始文档**为生成的**情境描述**提供背景参考。
- 生成的**情境描述**可能包括遇到问题的背景、现象等初步信息，必须忠于**先验知识**和**原始文档**，不能随意编造或添加任何没有出现的内容。
- 运维任务可能是：检测（即描述现象判断系统是否存在事件或故障）、分诊（确定事件或故障大类）、定位（确定事件或故障具体类型）、缓解（推荐缓解措施或步骤）。

### 示例
**先验知识**：
事件大类是"ARP"，事件具体类型是"合法用户的ARP表项被修改"。

**原始文档**：
$${EXAMPLE_DOCS}$$

**情境描述**：
$${EXAMPLE_SCEN}$$

### 输出
给定的**先验知识**：事件大类是{COMM}，事件具体类型是{INCIDENT}。

给定的**原始文档**：{RAW_DOCS}

- 根据给定**先验知识**和**原始文档**，生成一个简短的**情境描述**。
- 在 </think> 之前充分思考, </think> 之后严格遵循指定格式
- 输出的开头和结尾用 $$ 符号隔开。
$$ $$
'''.strip()

# triage / localization
RANK_ANSWER = '''
### 任务描述
你是一位专业的语言理解与逻辑推理模型，负责参考给定的相关知识，判断描述所属的类型。
- 你将收到
  1. **问题描述**
  2. **候选列表**（待排序的列表，**问题描述**属于其中一个类型）
  3. **参考知识**（根据**问题描述**在知识库中检索到的相关内容，但也有可能存在部分错误，合理利用。按照相关性从前到后排列）
- 你的任务是：
  1. 理解**问题描述**和按照相关性从前到后排列的**参考知识**，可能需要初步筛选掉不相关或错误的知识。
  2. 结合筛选后的**参考知识**，评估**问题描述**属于**候选列表**中每项的相对概率。
  3. 按照相对概率的大小，输出从高到低排序**候选列表**；如果**候选列表**内元素数量大于十，则输出概率最高的前十个元素组成的列表即可。

### 输入
  - **问题描述**：{DESCRIPTION}
  - **候选列表**：{CANDIDATE_LIST}
  - **参考知识**：{REFERENCE}

### 输出
- 输出排序后的**候选列表**，列表需包含概率最高的前十个元素。
- 在 </think> 之前进行充分思考；</think> 之后，严格按照以下格式输出。
- 用 $$ 将整个列表包裹，十个字符串均用英文双引号包裹。
$$ ["", "", "", "", "", "", "", "", "", ""] $$
'''.strip()

WORKFLOW_GENERATION = '''
### 任务描述
你是一个 workflow 编排助手，负责参考相似历史经验为用户问题 query 编排一个排障流程。
- 你将收到：
  1. **用户查询 query**
  2. **相似故障节点列表 node list** (列表按与 query 的相关性从高到低排序；每个节点代表一类已知故障，包含所属类别、故障描述、根因总结、标准排障流程，主要提供排障的思路骨架；列表靠后的节点可能属于噪声，但也可能包含有用信息，需要甄别使用)
  3. **文档记录 doc list** (运维原始文档列表，与故障节点列表对应，是对节点中偏总结性质信息的补充，包含更完整的内容；但也包含一些噪声)
- 你需要按照以下推理步骤完成任务，且必须每个步骤开始前标出 Step1，Step2，Step3，Step4：
  Step1: **结构化理解**：将故障节点的 process 中每一步逐一拆解成三元组 (action, intention, context)，形成便于复用的操作。
  Step2: **子任务分解、操作匹配与编排**：把 query 对应的排障拆解为最小粒度的原子化排障目标，并为每个目标在操作三元组中匹配或生成合适的 action，以 node 中内容为骨架构建 workflow 的初步思路。该步骤可能需要融合多个相关 node 的思路。
  Step3: **细节完善**：在文档记录中查找与每个步骤相关的内容，补充细节（命令、操作步骤表述），同时修正或扩展上一步的初步 workflow。
  Step4: **反思并组织输出**：反思合理性，避免出现给你的 node 和 doc 之外的内容，输出最终的 workflow，使用 **Mermaid 语法** 描述。

### 输入
  - **用户查询**：{QUERY}
  - **相似故障节点列表**：{NODE_LIST}
  - **文档记录列表**：{DOC_LIST}

### 输出要求
- 在 </think> 之前，遵循给定步骤推理，但不输出。在 </think> 之后，仅输出一个 Mermaid 块。
```mermaid
[在此输出最终的workflow]
```
'''.strip()

WORKFLOW_GENERATION_WO_STRUCTURE = '''
### 任务描述
你是一个 workflow 编排助手，负责参考相似历史经验为用户问题 query 编排一个排障流程。
- 你将收到：
  1. **用户查询 query**
  2. **相似故障节点列表 node list** (列表按与 query 的相关性从高到低排序；每个节点代表一类已知故障，包含所属类别、故障描述、根因总结、标准排障流程，主要提供排障的思路骨架；列表靠后的节点可能属于噪声，但也可能包含有用信息，需要甄别使用)
  3. **文档记录 doc list** (运维原始文档列表，与故障节点列表对应，是对节点中偏总结性质信息的补充，包含更完整的内容；但也包含一些噪声)

### 输入
  - **用户查询**：{QUERY}
  - **相似故障节点列表**：{NODE_LIST}
  - **文档记录列表**：{DOC_LIST}

### 输出要求
- 在 </think> 之后，仅输出一个 Mermaid 块。
```mermaid
[在此输出最终的workflow]
```

### 你的思维过程及答案 
'''.strip()

SCORE_GENERATION = '''
### 任务描述
你是一位 workflow 质量评估专家，需要参考用户查询、标准 workflow，对生成workflow进行多维度评分。
- 你将收到：
  1. **用户查询 query**
  2. **标准工作流 STANDARD_WORKFLOW**
  3. **生成工作流 gen_workflow**
- 你需要按照以下推理步骤完成任务，且必须每个步骤开始前标出 Step1，Step2，Step3，Step4：
  Step1: **准确性分析**：按照步骤，逐一对比生成工作流与标准工作流，判断操作步骤、参数、命令等是否一致或等价。  
  Step2: **完整性分析**：确认生成工作流是否覆盖了标准工作流中的所有关键步骤，没有明显缺漏或跳步，且能解决用户的关键需求。
  Step3: **清晰度分析**：评估每个步骤是否表达清晰且实际可执行。
  Step4: **反思与评分**：反思上述分析的合理性，据此给出 "accuracy"、"completeness"、"clarity" 三个维度分数，每个维度分数为 0-5 的整数，最终输出 **json** 字典。
  
### 输入
  - **用户查询**：{USER_QUERY}
  - **标准工作流**：{REF_WORKFLOW}
  - **生成工作流**：{GEN_WORKFLOW}

### 输出要求
- 在 </think> 之前，遵循给定步骤推理，但不输出。
- 在 </think> 之后，仅输出一个 JSON 字典（键为 "accuracy"、"completeness"、"clarity"，取值为 0-5 的整数），禁止包含任何额外文本。
```json
{{
  "accuracy": [在此输出 0-5 的整数分数],
  "completeness": [在此输出 0-5 的整数分数],
  "clarity": [在此输出 0-5 的整数分数]
}}
```
'''.strip()

RANK_TEST = '''
### 任务描述
你是一位专业的语言理解与逻辑推理模型，负责参考给定的相关知识，判断描述所属的类型。你将在一次推理中同时完成以下两个任务：
1. **Triage（类型归属）**：判断问题描述属于哪个 community（即类别）。
2. **Localization（事件定位）**：在最相关的 community 中，进一步判断问题描述最可能对应的 incident（即具体故障事件）。

你将收到以下输入：
1. **问题描述**
2. **候选 community 列表**：待排序的列表，问题描述属于其中一个类型
3. **community-incident 映射关系**：每个 community 下包含若干 incident（即具体故障案例）。
4. **参考知识**：根据问题描述在知识库中检索到的相关内容，但也有可能存在部分错误，合理筛选与利用。按照相关性从前到后排列

### 你的任务
1. 理解问题描述和按照相关性从前到后排列的参考知识，可能需要初步筛选掉不相关或错误的知识；
2. 结合筛选后的参考知识，评估问题描述属于community候选列表中每项的相对概率；
3. 根据评估结果，确定最可能的前10个 community；
4. 根据community-incident 映射关系，获取前10个community 下的 incident ，得到候选 incident列表；
5. 结合筛选后的参考知识，评估问题描述属于 incident 候选列表中每项的相对概率；
6. 输出：
   - 按照相对概率的大小，输出从高到低排序的前 10 个最可能的 community；
   - 按照相对概率的大小，输出从高到低排序的前 10 个最可能的 incident（可来自不同 community）。

### 输入
- **问题描述**：{DESCRIPTION}
- **候选 community 列表**：{COMMUNITY_LIST}
- **community-incident 映射关系**：{COMMUNITY_INCIDENT_MAP}
- **参考知识**：{REFERENCE}

### 输出
- 在 </think> 之前进行充分思考；</think> 之后，严格按照以下格式输出。
- 请严格按照以下格式输出：
$$
{{
  "community_rank": ["", "", "", "", "", "", "", "", "", ""],
  "incident_rank": ["", "", "", "", "", "", "", "", "", ""]
}}
$$
'''.strip()

RANK_TEST_2 = '''
### 任务描述
你是一位专业的语言理解与逻辑推理模型，负责参考给定的相关知识，判断描述所属的类型。你将在一次推理中同时完成以下两个任务：
1. Triage（community 归属）：在给定的候选 community 中判断问题描述最可能属于哪些 community。
2. Localization（incident 定位）：基于给定的候选 incident ，在最相关的 community 中，进一步判断问题描述最可能对应的 incident（即具体故障事件）。

你将收到以下输入：
1. **问题描述**
2. **候选 community 列表**：待排序的 community 列表，问题描述属于其中一个类型
2. **候选 incident 列表**：待排序的 incident 列表，问题描述属于其中一个类型
3. **community-incident 映射关系**：每个 community 下包含若干 incident（即具体故障案例）
4. **community 参考知识**：根据问题描述在知识库中检索到的相关 community 内容，但也有可能存在部分错误，合理筛选与利用。按照相关性从前到后排列
4. **incident 参考知识**：根据问题描述在知识库中检索到的相关 incident 内容，但也有可能存在部分错误，合理筛选与利用。按照相关性从前到后排列

 
### 输入
- **问题描述**：{DESCRIPTION}  
- **候选 community 列表**：{COMMUNITY_LIST}  
- **候选 incident 列表**：{INCIDENT_LIST}  
- **community -> incident 映射**：{COMMUNITY_INCIDENT_MAP}  
- **community 参考知识**：{COMMUNITY_REFERENCE}  
- **incident 参考知识**：{INCIDENT_REFERENCE}  

### 你的任务
1. 理解问题描述和按照相关性从前到后排列的参考知识，可能需要初步筛选掉不相关或错误的知识；
2. 结合筛选后的参考知识，使用 {COMMUNITY_LIST} 与 {COMMUNITY_REFERENCE} 中的内容评估问题描述属于community候选列表中每项的相对概率；
3. 根据评估结果，确定最可能的前10个 community；
4. 根据community-incident 映射关系，获取前10个community 下的 incident ，得到候选 incident列表；
5. 结合筛选后的参考知识，使用 {INCIDENT_LIST} 与 {INCIDENT_REFERENCE} 评估问题描述属于 incident 候选列表中每项的相对概率；
6. 输出：
   - 按照相对概率的大小，输出从高到低排序的前 10 个最可能的 community；
   - 按照相对概率的大小，输出从高到低排序的前 10 个最可能的 incident（可来自不同 community）。 

### 输出
- 在 </think> 之前进行充分思考；</think> 之后，严格按照以下格式输出。
- 请严格按照以下格式输出：
$$
{{
  "community_rank": ["", "", "", "", "", "", "", "", "", ""],
  "incident_rank": ["", "", "", "", "", "", "", "", "", ""]
}}
$$
'''.strip()

RANK_TEST_3 = '''
### 任务描述
你是一位专业的语言理解与逻辑推理模型，负责参考给定的相关知识，判断描述所属的类型。你将在一次推理中同时完成以下两个任务：
1. Triage（community 归属）：在给定的候选 community 中判断问题描述最可能属于哪些 community。
2. Localization（incident 定位）：基于给定的候选 incident ，在最相关的 community 中，进一步判断问题描述最可能对应的 incident（即具体故障事件）。

你将收到以下输入：
1. **问题描述**
2. **候选 community 列表**：待排序的 community 列表，问题描述属于其中一个类型
2. **候选 incident 列表**：待排序的 incident 列表，问题描述属于其中一个类型
3. **community-incident 映射关系**：每个 community 下包含若干 incident（即具体故障案例）
4. **community 参考知识**：根据问题描述在知识库中检索到的相关 community 内容，但也有可能存在部分错误，合理筛选与利用。按照相关性从前到后排列
4. **incident 参考知识**：根据问题描述在知识库中检索到的相关 incident 内容，但也有可能存在部分错误，合理筛选与利用。按照相关性从前到后排列

 
### 输入
- **问题描述**：{DESCRIPTION}  
- **候选 community 列表**：{COMMUNITY_LIST}  
- **候选 incident 列表**：{INCIDENT_LIST}  
- **community -> incident 映射**：{COMMUNITY_INCIDENT_MAP}  
- **community 参考知识**：{COMMUNITY_REFERENCE}  
- **incident 参考知识**：{INCIDENT_REFERENCE}  

### 你的任务
1. 理解问题描述及参考知识，并初步筛选掉不相关或错误的信息；
2. 结合筛选后的参考知识，利用 community 列表及 community 参考知识，判断问题描述最可能属于哪些 community，并按照可能性的高低给出排序后的前 10 个 community；
3. 根据community-incident 映射关系，获取前10个community 下的 incident 作为候选；
4. 结合筛选后的参考知识，利用 incident 列表及 incident 参考知识，判断问题描述最可能属于哪些 incident，并按照可能性的高低给出排序后的前 10 个 incident。

### 输出
- 在 </think> 之前进行充分思考；</think> 之后，严格按照以下格式输出。
- 请严格按照以下格式输出：
$$
{{
  "community_rank": ["", "", "", "", "", "", "", "", "", ""],
  "incident_rank": ["", "", "", "", "", "", "", "", "", ""]
}}
$$
'''.strip()

