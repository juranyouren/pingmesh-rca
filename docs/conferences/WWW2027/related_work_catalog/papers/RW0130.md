# RW0130 — APGNN: Alarm Propagation Graph Neural Network for fault detection and alarm root cause analysis

| 字段 | 内容 |
|---|---|
| paper_id | RW0130 |
| 完整题名 | APGNN: Alarm Propagation Graph Neural Network for fault detection and alarm root cause analysis |
| 作者 | W. Jiang, Y. Bai（北航） |
| venue | Computer Networks（Elsevier），vol. 220, art. 109485 |
| 类型 / 年份 | 期刊 / 2022（Crossref 记录；仓库旧材料记 2023，以一手记录为准） |
| DOI | [10.1016/j.comnet.2022.109485](https://doi.org/10.1016/j.comnet.2022.109485) |
| CCF 等级 | **B**（CCF 第七版，`journals/cn` = Computer Networks） |
| 层次 / 主题 | L1 / 事件依赖与传播建模 |
| 证据深度 | **仅元数据**。全文**未取得**（T007 轮次已确认：OpenAlex `is_oa: false`、`any_repository_has_fulltext: false`；ScienceDirect/ACM/机构仓库均无 OA 全文） |
| 最后核验 | 2026-09-15 |

> ⚠️ **本卡内容极为有限，谨慎引用。** 除题名、作者、venue、DOI 外，
> 本卡**没有任何已核实的原文事实**。T007 验收 R1 明确要求：APGNN 只有摘要，
> 「是否报告图结构指标」应为 **UNKNOWN**，**不能给确定的「无」**。

## 1. 从题名可确认的信息（不构成能力判断）

题名表明该工作把**告警传播**建模为**图**，并用 **GNN** 处理两个任务：
故障检测 + 告警根因分析。

**「Alarm Propagation Graph」这一命名本身说明：在网络运维领域，
「告警传播图」是一个已被使用的研究对象。** 这对我方 related work 有直接含义：
不能声称「把告警组织成传播图」是本文首创。

## 2. 与 RPG-Recon 的关系（待核实）

题名层面与本项目输出对象高度接近（告警传播图 + 根因）。但其：
- 节点粒度（告警？设备？）
- 边定义（传播关系如何确定）
- 是否输出可逐边评价的图
- 评价指标

**全部为 UNKNOWN**。

## 3. 已知的指标线索（**置信度 low，不足以支撑结论**）

T007 轮次记录：摘要中唯一的量化表述是
「average **4.6% in F1-score**」，语境为 **APG → 真实故障的映射（整图二分类）**。
由于正文不可得，该判断 **置信度 low**，**不得**用于声称该文采用了或未采用图结构指标。

## 4. 为什么仍然收录

按任务规则「没有本项目所需输入或代码的论文仍可高度相关」。该文是**网络运维领域
明确以「告警传播图」为对象**的工作，属于 L1 必须列出并标注全文缺口的条目。
缺失全文是一个**具体、可执行的后续动作**（见第 6 节），不是收录理由的削弱。

## 5. 可能的审稿质疑（对我方）

- 「APGNN 已经用 GNN 做告警传播图的根因分析，你的方法差异在哪？」
  **本轮无法回答**——必须先取得全文。这是当前目录中**最需要补全文**的条目之一。

## 6. 后续动作（具体）

1. 通过机构订阅（IEEE/Elsevier）取得 Computer Networks vol. 220 art. 109485 全文；
2. 重点核实：节点/边定义、是否评价图结构、指标口径、参考图来源；
3. 取到后更新本卡，并把 `内容阅读深度` 从「仅元数据」升级。

## 7. 未核实项

除身份外**全部 UNKNOWN**：节点粒度、边定义、输出形式、评价指标、数据来源、
代码可得性。
