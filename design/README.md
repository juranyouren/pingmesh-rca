# Design Docs

This folder now keeps only the active design direction.

- `trust_tree_gate_design.md`: rule-based topo/temporal trust-tree gate, online routing contract, and server evaluation commands.

Old CREDENCE calibration artifacts were removed because the project moved from
continuous confidence scores to auditable logical trust trees.
